 /**
     * 盛水最多的容器
     * 容器面积应该是长乘高，这里长度我们可以不断地枚举，高度则是每个坐标的值，并且与面积有关的高度
     * 应该是最短的高度
     * 1.可以不断的枚举不同的长度，从1-n，取不同的高度进行枚举
     * 2.但这里我们求得是最大的面积，所以一开始就直接取最长的 n然后不断地进行高度的选取
     * 3.因为高度只与最小的高度有关，所以不改变最长的一个高，不断地移动最短的边
     * 这里举个例子
     * 8.。。。。100。。。。。9这里。代表一个数字
     * 对于100而言，对于他来讲，最长最长的情况，也就到首尾了
     *跳过这道题
